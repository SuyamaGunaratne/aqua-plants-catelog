services:
  aquarium-catalog:
    image: aquarium-catalog:${IMAGE_TAG:-0.1.0}
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      NEXT_TELEMETRY_DISABLED: "1"
    restart: unless-stopped